
using UnityEngine;

public class Billboard3D : MonoBehaviour
{
    readonly static Vector3 vUp = new Vector3(0, 1, 0);
    
    void Update()
    {
        // Vector3 camera_pos = FollowingCamera.Singleton().transform.position;
        
        // transform.LookAt(camera_pos, vUp);
    }
}
